VITE_API_BASE=http://localhost:3000

# SourceCheck no longer uses a shared client secret.
# Deployed backends should use backend-issued session tokens (SESSION_SECRET)
# via POST /api/session/init. The extension handles this automatically.

# Optional: extension-side timeout for backend API calls in milliseconds.
# VITE_REQUEST_TIMEOUT_MS=20000
